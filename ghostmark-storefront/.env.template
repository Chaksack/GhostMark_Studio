# Ghostmark Storefront – Environment Template
# Copy this file to .env.local and fill in the values as needed.

# --- Medusa Backend ---
# URL to your Medusa backend (Store API). Typically http://localhost:9000 for local dev.
MEDUSA_BACKEND_URL=http://localhost:9000

# Publishable API key for Medusa Store API (if your backend requires it)
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=

# Default region/country for the storefront routing (2-letter code, e.g., us, de, fr)
NEXT_PUBLIC_DEFAULT_REGION=us

# --- Stripe (in-site Elements + Checkout Session) ---
# One of the following publishable keys must be set to enable Stripe client SDK:
# Use native Stripe key OR Medusa Payments publishable key if you use Medusa Payments.
NEXT_PUBLIC_STRIPE_KEY=
NEXT_PUBLIC_MEDUSA_PAYMENTS_PUBLISHABLE_KEY=

# Server-side secret for creating Stripe Checkout Sessions via the API route.
# Use native Stripe key OR Medusa Payments secret key if you use Medusa Payments.
STRIPE_SECRET_KEY=
MEDUSA_PAYMENTS_SECRET_KEY=

# Optional: If you use a connected (destination) account, set the Stripe Account ID.
NEXT_PUBLIC_MEDUSA_PAYMENTS_ACCOUNT_ID=

# --- Stripe Payment Link (optional shortcut) ---
# If set, the cart's "Go to checkout" button will redirect directly to this link.
# Provided by user for testing purposes; replace with your own Payment Link as needed.
NEXT_PUBLIC_STRIPE_PAYMENT_LINK_URL=https://buy.stripe.com/test_7sY14pbbx8Ov8Q929K3ks00

# --- Misc / Deployment ---
# Base URL used by some utilities; optional.
NEXT_PUBLIC_BASE_URL=

# Optional (used by sitemap generation)
NEXT_PUBLIC_VERCEL_URL=
